#include<iostream>
using namespace std
I/ Node structure for singly linked list struct Node ( int data;
Node* next;
// Function to create a new node
ode* createNode(int value) {
lode* newode - new Node() newode-›data - value; newode-›next - nullptr;
return newlode
// Function to insert node at the end of the list
void insertEnd (Node*& head, int value) ( Node* newlode - createlode(value);
void insertend(Node & head, int value) {
head - neutlode;
1 else 1
Node* temp - head;
while (temp-›next I-nullptr) ( temp - temp-›next;
temp-›next - newlode;
Function to display the linked list
void displayList(Node* head) {
Node* temp = head;
cout « "Linked List: "; while (temp I-nullptr) (
cout ‹temp->data «;
temp - temp->next;
cout « endl;
int main() (
Node* head - nullptr;
ant n, value,
cout « "How many nodes do you want to create? "; cin >> n;
for (int i - e; i < n; +i) (
cout « "Enter value for node " « 1 + 1«": ";
void displaylist (Node* head) (
cout « endl;
int main() {
Node* head - nullptr; int n, value;
cout « "How many nodes do you want to create? ";
for (int 1 - 0; 1 ‹ n; +1) (
cout « "Enter value for node " « 1 + 1 « "; ";
Cin › value;
insertEnd (head, value);
displayList(head);
// Free memory
Node* temp;
while (head!- nullptr) {
temp - head;
head - head->next;
delete temp;
}
return 0;
}
