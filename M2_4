#include ‹lostream›
#include ‹queue›
using namespace std;
int main() (
queue int 4; int n, val;
cout « "Enter number of elements to enqueue: "; cin >> n;
cout « "Enter " « n « " elements: \n"; for (int 1 - 0; 1 < n; ++1) (
cin >> val; q-push(val);
If (1q.empty()) {
cout « "Front element: " ‹< q.front() « endl; cout « "Rear element: " « q-back() « endl;
} else (
cout « "Queue is empty. In";
return 0;
